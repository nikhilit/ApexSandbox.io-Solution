public Boolean isPrime(Integer num) {
    Integer count=0;

    for(Integer i=1;i<=num;i++)
    {
        if(math.mod(num,i)==0)
        {
            count++;
        }
    }
    if(count==2)
    {
        return true;
    }
    return false;
}